vendor
.idea
phpstan.neon
/composer.lock

# for tests，由于 webman 1.6.9 之后 BASE_PATH 不好调整，因此只能被放到项目级别
/config
/runtime
/app
/plugin
/resource
/TODO

node_modules

*.local.*
.worktree
.claude
.agents
.phpunit.result.cache
# e2e 测试生成的可抛弃应用（外部骨架代码，升级时删除重建）
/e2e/webman/
/e2e/laravel/
